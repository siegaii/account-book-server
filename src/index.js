// const app = require('./app.js');
const { initDB } = require('./DB');

// const port = 9999;
// 初始化数据库
initDB();

// app.listen(port, () => {
//   console.log(`请访问 http://127.0.0.1:${port}`);
// });
